{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "pnpm dev",
      "cwd": "${workspaceFolder}",
      "name": "Vite dev server",
      "request": "launch",
      "type": "node-terminal"
    }
  ]
}
