---
/**
 * AppLayout — primary shell for all main app routes.
 *
 * Structure:
 *   ┌─────────────────────────────────────┐
 *   │ AppHeader (brand + NavMenu + title) │
 *   ├─────────────────────────────────────┤
 *   │ <slot />                            │
 *   ├─────────────────────────────────────┤
 *   │ AppFooter                           │
 *   └─────────────────────────────────────┘
 *
 * Props:
 *   title   — passed to AppHeader and <title> tag
 */
import AppFooter from 'components/AppFooter/AppFooter.astro';
import AppHeader from 'components/AppHeader/AppHeader.astro';
import { Toaster } from 'components/ui/sonner';
import BaseLayout from './BaseLayout.astro';

interface Props {
  title?: string;
  /** Remove page padding and clip overflow — for full-height split views (e.g. vault transcripts). */
  fullBleed?: boolean;
}

const { title, fullBleed = false } = Astro.props;
const pageTitle = title ? `${title} — LLAAB` : 'LLAAB';
---

<BaseLayout title={pageTitle}>
  <div class="app-shell">
    <AppHeader title={title} />

    <main class="page-content" data-bleed={fullBleed ? 'true' : undefined}>
      <slot />
    </main>

    <AppFooter />
  </div>

  <Toaster client:only="react" />
</BaseLayout>

<style>
  .app-shell {
    display: flex;
    flex-direction: column;
    min-height: 100dvh;
    width: 100%;
    background: var(--bg);
  }

  .page-content {
    display: flex;
    flex: 1;
    flex-direction: column;
    min-height: 0;
    padding: 1.5rem;
    overflow-y: auto;
  }

  .page-content[data-bleed='true'] {
    padding: 0;
    overflow: hidden;
  }
</style>
